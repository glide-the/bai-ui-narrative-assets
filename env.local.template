# Copy to .env.local: cp env.local.template .env.local
# Postgres for root docker-compose.yml (host port 5433, not 5432)
DATABASE_URL=postgres://nationality:nationality@localhost:5433/nationality
PGHOST=localhost
PGPORT=5433
PGUSER=nationality
PGPASSWORD=nationality
PGDATABASE=nationality

# See .env.local.example for Anthropic, storage, WeKnora, and other variables.
